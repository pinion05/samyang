#spring.application.name=firstProject
#spring.application.datasource.driver-class=com.mysql.cj.jdbc.Driver
#properties 방식
#yml 방식
#url 뒤에 붙는 ? 는 매개변수
spring:
 application:
#프로젝트명
  name: 'samyang'
  #접속할 데이터베이스 설정
 datasource: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://34.47.105.223/farm404?charsetEncoding=utf-8
  username: root
    password: '6VNS*Z$ARf~j17e='
 mvc:
  view:
   prefix: /views/
   suffix: .jsp
   
mybatis:
 mapper-locations: classpath:/mapper/*.xml
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true
  web:
    resources:
      cache:
        period: 0
